/*import {ROLLUP_MENU, USER_LOGIN, IS_PRELOADER, GET_REPORTS} from '../actions/constants';*/

const initialState = {
    isMenuRollUp: false,
    isPreloader: false,
    userData: {},
    reports: []

};

export const mainReducer = (state = initialState, action) => {
    switch (action.type) {
    }
    return state;
};